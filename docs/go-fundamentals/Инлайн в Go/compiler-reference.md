# Встраивание функций: устройство компилятора Go 1.27

> **Проверено:** Go 1.27 · **Уровень:** младший разработчик — углублённый
> Решения компилятора не являются гарантией языка и должны перепроверяться на целевой версии.

## 1. Что такое инлайн (inline) в Go?

**Инлайн (inlining)** — это оптимизация компилятора, при которой вызов функции заменяется непосредственно её телом.

Проще говоря, компилятор «копирует» код из вызываемой функции и «вставляет» его на место вызова, вместо того чтобы генерировать инструкции для перехода к другой части кода.

```go
func main() {
    result := add(2, 3)
    println(result)
}

func add(a, b int) int {
    return a + b
}

```

Здесь при выполнении `main` происходит вызов `add`: процессор перепрыгивает в код функции `add`, выполняет сложение и возвращается назад.

```go
func main() {
    // Вызов функции add заменен на ее тело
    result := 2 + 3 // <-- Вот это и есть инлайн
    println(result)
}

func add(a, b int) int {
    return a + b
}

```

### Зачем это нужно?

* **Устранение накладных расходов:** прямой вызов, возврат и часть работы с
  аргументами или кадром могут исчезнуть. Стоимость нельзя задавать «десятками
  наносекунд» без процессора, версии и теста.
* **Открытие возможностей для других оптимизаций:** Когда код находится «в одном месте», компилятор может применить к нему более агрессивные оптимизации: **constant folding** (постоянное свёртывание), **dead code elimination** (удаление мёртвого кода), лучший анализ потоков данных.
* **Компромисс размера кода:** исчезновение вызова может помочь часто
  выполняемому пути, но дублирование тела увеличивает двоичный файл и иногда
  ухудшает локальность кэша инструкций. Поэтому компилятор учитывает размер и профиль.

> **Зачем это Go-разработчику.** Инлайн напрямую влияет на производительность вашего кода. Маленькие вспомогательные функции (геттеры, простые преобразования, обёртки) — идеальные кандидаты. Пишите их компактно, и компилятор сделает остальное.

### Ограничения

* Инлайн применяется в основном к маленьким и простым функциям. Сложные функции (с циклами, сложными условиями, вызовами других функций) обычно не инлайнятся, так как это может привести к сильному «разбуханию» бинарного кода.
* **Рекурсивные функции** не инлайнятся — компилятор не может заменить рекурсивный вызов телом функции, так как это привело бы к бесконечному разворачиванию. Go также не поддерживает оптимизацию хвостовой рекурсии.
* **Методы интерфейсов** не инлайнятся при вызове через интерфейсную переменную, поскольку компилятор не знает конкретный тип на этапе компиляции. Исключение — **девиртуализация** (Go 1.21+), когда компилятор доказывает конкретный тип и заменяет интерфейсный вызов прямым.
* **Функции с ****`defer`** имеют повышенную стоимость из-за дополнительной логики разворачивания стека, что снижает шансы на инлайн.
* Решение принимает компилятор. Директива `//go:noinline` запрещает оптимизацию;
  открытой директивы `//go:inline` в Go нет.

***

## 2. Когда компилятор делает инлайн

Компилятор не может инлайнить всё подряд. Ему нужна метрика, чтобы определить, будет ли инлайн выгоден и не приведёт ли он к чрезмерному росту размера программы. Этой метрикой и являются **«веса» (weights)** или **«стоимость» (cost)**.

### Что такое «вес» функции?

Это численное значение, которое оценивает «сложность» функции с точки зрения компилятора. Чем выше вес, тем «тяжелее» функция и тем меньше шансов, что она будет инлайнирована.

### Как считается стоимость

Компилятор оценивает внутреннее представление функции, место вызова, ожидаемый
выигрыш и рост кода. Конкретные веса меняются между выпусками и не являются
открытым API. Таблица с фиксированной ценой `if`, `for` или вызова быстро
устаревает и не должна определять стиль.

Разделяйте функции по ответственности и читаемости. Если профиль указывает на
частый вызов, смотрите фактическое решение через `-m=2`; не переписывайте код
под предполагаемый счётчик синтаксического дерева.

### Порог инлайнинга

У компилятора есть внутренние пределы, но одного устойчивого порога «80» для
всех мест вызова нет. Эвристики учитывают контекст и PGO; проверяйте их на своей
версии Go.

***

## 3. Как узнать, что компилятор заинлайнил функцию?

Основной инструмент — флаг `-gcflags="-m"`, который включает вывод решений компилятора об оптимизациях:

```shellscript
go build -gcflags="-m" ./...

```

Вывод покажет:

```

./main.go:12:6: can inline add
./main.go:8:6: can inline main
./main.go:9:5: inlining call to add

```

* `can inline add` — компилятор считает функцию `add` достаточно лёгкой для инлайна.
* `inlining call to add` — вызов `add` действительно был заинлайнен в месте вызова.
* `function too complex for inlining` — функция слишком сложна, инлайн не применяется.

Для более подробного вывода используйте `-gcflags="all=-m=2"`.

Глубокий анализ через SSA-дамп

Для просмотра SSA конкретной функции компилятор умеет создавать HTML-отчёт:

```shellscript
GOSSAFUNC=main go build

```

Откроется `ssa.html` в браузере, где визуально видно все этапы компиляции, включая этап инлайнинга.

> **Зачем это Go-разработчику.** Всегда проверяйте `-gcflags="-m"` при оптимизации узких мест. Вы можете быть уверены, что ваша маленькая функция заинлайнится, а на деле — нет (из-за скрытой сложности вроде `defer` или вызова другого метода). Не гадайте — проверяйте.

***

## 4. Управление инлайнингом: директивы компилятора

Для пользовательского кода есть директива, запрещающая встраивание. Директивы,
заставляющей компилятор встроить функцию, нет.

### `//go:noinline` — запрет инлайна

Категорический запрет компилятору инлайнить функцию. Применяется непосредственно перед объявлением функции:

```go
//go:noinline
func expensiveDebug(x int) int {
    return x * 2
}

```

Применяйте её редко: в тестах компилятора или среды выполнения, в эксперименте,
где измеряется именно граница вызова, либо как подтверждённый обход ошибки.
Обычный тест производительности должен измерять оптимизированный рабочий код;
`//go:noinline` способен сделать результат менее реалистичным.

### Директивы `//go:inline` не существует

Комментарий `//go:inline` не является директивой компилятора и ничего не
рекомендует оптимизатору. Используйте ясный код, PGO и проверку диагностики.

***

## 5. Встраивание по цепочке вызовов (Go 1.12+)

До Go 1.12 компилятор инлайнил только **листовые (leaf) функции** — те, которые сами не вызывают других функций. Это серьёзно ограничивало применимость оптимизации.

Начиная с Go 1.12, появился **mid-stack inlining** — возможность инлайнить функцию, находящуюся не только на конце, но и в середине цепочки вызовов.

### Как это работает

Рассмотрим цепочку: `A → B → C → D`, где `D` — листовая функция.

* **Без mid-stack (Go < 1.12):** инлайнится только `D`.
* **С mid-stack (Go ≥ 1.12):** `D` встраивается в `C`, затем `C` (уже с учётом встроенной `D`) может быть встроена в `B`, и так далее. Инлайн идёт «волной» от листьев вверх по стеку.

### Предел встраивания по цепочке

Допустимость каждого места вызова зависит от текущего предела и контекста.
Сравнение «~200 против ~80» не является контрактом и быстро устаревает вместе с
оптимизатором и эвристиками PGO.

### Оптимизация по профилю

Если сборка получает профиль процессора через `go build -pgo=...`, компилятор
может активнее встраивать частые вызовы и экономить размер кода на редких путях.
PGO не заменяет измерения: сравнивайте представительные тесты и профили до и
после на одной версии набора инструментов.

> **Зачем это Go-разработчику.** Встраивание по цепочке позволяет сохранять
> небольшие вспомогательные функции без ручного разворачивания, но ничего не
> гарантирует для конкретного вызова. Проверяйте диагностику оптимизированной
> сборки и тест производительности.

***

## 6. Связь инлайна с escape analysis и другими оптимизациями

Инлайн не существует изолированно — он тесно связан с другими механизмами компилятора и часто служит «катализатором» для более глубоких оптимизаций.

### Инлайн и escape analysis

**Escape analysis** определяет, выделяется ли переменная в куче (heap) или на стеке (stack). Инлайн может кардинально изменить его результат.

Рассмотрим пример:

```go
func newInt() *int {
    x := 42
    return &x // x «убегает» в кучу
}

func main() {
    p := newInt()
    fmt.Println(*p)
}

```

Без инлайна `x` выделяется в куче — она «убегает» из `newInt`. Но если `newInt` заинлайнится в `main`, компилятор увидит полный контекст использования и может доказать, что `x` не живёт дольше `main`. В результате `x` останется на стеке — выделения в куче не будет.

### Инлайн и девиртуализация (Go 1.21+)

**Девиртуализация (devirtualization)** — это замена вызова метода через интерфейс прямым вызовом конкретного типа. Если компилятор может доказать, какой конкретный тип скрывается за интерфейсом, он:

1. Заменяет интерфейсный вызов прямым.
2. После этого прямой вызов может быть заинлайнен.

```go
var w io.Writer = os.Stdout
w.Write([]byte("hello")) // интерфейсный вызов

```

Если компилятор докажет, что `w` — это всегда `*os.File`, вызов `Write` будет девиртуализован и потенциально заинлайнен.

### Инлайн и constant propagation

После инлайна компилятор видит фактические значения аргументов и может:

* Вычислить выражение на этапе компиляции (constant folding).
* Удалить ветки кода, которые никогда не выполняются с данными аргументами (dead code elimination).

> **Зачем это Go-разработчику.** Инлайн — это «ключ», открывающий дверь для других оптимизаций. Функция может быть тривиальной сама по себе, но её инлайн позволяет компилятору увидеть общую картину и применить каскад оптимизаций: исключить выделение в куче, вычислить константы, удалить мёртвый код.

***

## 7. Влияние на отладку и стектрейсы

Компилятор записывает сведения о встроенных функциях. Современные стеки вызовов,
`runtime.CallersFrames` и pprof умеют раскрывать логические кадры, поэтому
функция не обязана исчезать из диагностики.

### Как инлайн меняет стектрейс

Рассмотрим цепочку вызовов `main → process → validate`, где `validate` заинлайнен в `process`:

**Стектрейс без инлайна:**

```

goroutine 1 [running]:
main.validate(...)
    /app/main.go:25
main.process(...)
    /app/main.go:15
main.main()
    /app/main.go:8

```

**Стектрейс с инлайном ****`validate`****:**

```

goroutine 1 [running]:
main.process(...)
    /app/main.go:15
main.main()
    /app/main.go:8

```

Показанный сокращённый стек не является общим поведением современного Go:
обычная трассировка паники способна показать `validate(...)` как встроенный кадр
и правильную строку исходного кода. Обход адресов команд должен использовать
`runtime.CallersFrames`, который корректно разворачивает эти сведения.

### Что показывает pprof

`go tool pprof` использует символы и сведения о встраивании для отнесения
образцов. При расхождениях проверяйте соответствие профиля двоичному файлу и
наличие отладочной информации, а не отключайте оптимизацию заранее.

### Практические рекомендации по отладке

* Для получения кадров из адресов команд используйте `runtime.CallersFrames`.
* Не ставьте `//go:noinline` на пути проверки и ошибок только ради трассировок.
* Используйте `GOTRACEBACK=system` для максимально подробного стектрейса.

> **Зачем это Go-разработчику.** Используйте инструменты, понимающие сведения о
> встраивании. Отключайте оптимизацию только для узкого эксперимента, а не как
> постоянную политику наблюдаемости.

***

## 8. Практические рекомендации

| Рекомендация                           | Пояснение                                                                                                        |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| Пишите ясные небольшие функции         | Одна ответственность и читаемость важнее попытки угадать внутренний лимит компилятора |
| Не злоупотребляйте `//go:noinline`     | Используйте для узкого эксперимента с компилятором, а не как общее средство для замеров и отладки |
| Проверяйте через `-gcflags="all=-m=2"` | Смотрите реальные решения компилятора в используемом наборе инструментов |
| Помните про компромисс скорость/размер | Встраивание может убрать вызов, но увеличить двоичный файл и нагрузку на кеш инструкций |
| Обновляйте Go                          | Каждая новая версия улучшает эвристики инлайнера — код, не инлайнившийся в 1.19, может инлайниться в 1.22        |
| Учитывайте сведения о встраивании       | Используйте `runtime.CallersFrames` и современные инструменты `pprof` и среды выполнения |
| Используйте `GOSSAFUNC` для анализа    | Визуализация SSA помогает понять, почему та или иная функция не была заинлайнина                                 |

> **Зачем это Go-разработчику.** Инлайн — это не магия и не то, что можно игнорировать. Понимание механизма позволяет писать код, который компилятор способен эффективно оптимизировать, и осознанно управлять этим процессом там, где это действительно нужно.

## 9. Проверяемый эксперимент

Встраивание оценивайте вместе со скоростью, размером двоичного файла и выделениями:

```bash
go test -run '^$' -bench BenchmarkLookup -benchmem -count=10
go test -c -o app.test
go build -gcflags='all=-m=2' ./cmd/app
```

Сравнивайте результаты статистическим инструментом, а не одной парой чисел.
Отдельная сборка с `-gcflags='all=-l'` полезна как контроль без встраивания, но она
меняет и последующие оптимизации, поэтому не изолирует стоимость одного вызова.

PGO может повысить бюджет встраивания для горячих рёбер графа вызовов и помочь
девиртуализации. Храните профиль, версию Go и исходную нагрузку вместе с результатом:
смена компилятора способна изменить решение без правки исходного кода.

Для корректного обхода кадров стека используйте `runtime.Callers` вместе с
`runtime.CallersFrames`: эта пара учитывает встроенные вызовы лучше, чем ручное
преобразование каждого программного счётчика через `runtime.FuncForPC`.

## Ссылки

- [Go Wiki: Compiler And Runtime Optimizations](https://go.dev/wiki/CompilerOptimizations)
- [Profile-guided optimization](https://go.dev/doc/pgo)
- [Документация `runtime.CallersFrames`](https://pkg.go.dev/runtime#CallersFrames)
